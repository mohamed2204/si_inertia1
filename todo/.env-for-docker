APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:QHivuxAFN+L/8O880XyZM1GwLbAuE9ENgbvC2GQqEXI=
APP_DEBUG=true
APP_URL=https://si-app.domain.lan

APP_LOCALE=fr
APP_FALLBACK_LOCALE=fr
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Connexion Docker MySQL / MariaDB
DB_CONNECTION=mysql
DB_HOST=laravel_db
DB_PORT=3306
DB_DATABASE=si_db
DB_USERNAME=si_user
DB_PASSWORD=si_user

SESSION_DRIVER=cookie
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# Gestion du cache (nécessite l'exécution des migrations)
CACHE_STORE=database

# Configuration Mail & IMAP (Ajustez les HOSTS selon vos conteneurs)
IMAP_HOST=127.0.0.1
IMAP_PORT=143
IMAP_ENCRYPTION=false
IMAP_VALIDATE_CERT=false
IMAP_USERNAME=masterdc@exemple.net
IMAP_PASSWORD=123456

TARGET_EMAIL=masterdc@exemple.net

MAIL_MAILER=smtp
MAIL_HOST=127.0.0.1
MAIL_PORT=587
MAIL_USERNAME=masterdc@exemple.net
MAIL_PASSWORD=123456
MAIL_ENCRYPTION=false
MAIL_FROM_ADDRESS=masterdc@exemple.net
MAIL_FROM_NAME="MasterDC"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"